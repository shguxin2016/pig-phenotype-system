电子表格导入导出原则
1 耳标号为唯一性字段，其它字段可以的重复
2 导入预校验时，库已有耳标号，如其它字段不一样，需要警示提醒，不要写入，如其它字段全部相同，则跳过此行记录继续下条记录，不要停止下一步的校验和写入。
3 库已没有耳标号的记录则校验通过后写入库中。
4 种类表格导出时，需要导出库中各表格中的所有字段，包括自动计算和统计填入的字段。
5 出错提示信息需要全面，需要标示出唯一性的耳标号字段信息。
6 导出时，筛选条件中添加品种字段。